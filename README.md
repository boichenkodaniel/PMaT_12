# Лабораторная работа № 12
## Разработка и тестирование REST API на Python

**Студент:** Бойченко Даниэль Дмитриевич  
**Группа:** 220032-11  
**Вариант:** 2

---

### Задания

| № | Формулировка |
|---|----------|
| 1 | Генерация CRUD-приложения |
| 2 | Генерация тестов |
| 3 | Рефакторинг «плохого» кода |
| 4 | Генерация Docker-конфигурации |
| 5 | Объяснение сложного кода |
| 6 | Генерация документации |

---

## Технологии

- **Язык:** Python 3.11+
- **Фреймворк:** FastAPI
- **Валидация:** Pydantic v2
- **База данных:** PostgreSQL 16
- **ORM:** SQLAlchemy 2.0
- **Миграции:** Alembic
- **Сервер:** Uvicorn
- **Тестирование:** pytest, httpx
- **Контейнеризация:** Docker, Docker Compose

---

## Структура проекта

```
app/
├── __init__.py      # Инициализация пакета
├── database.py      # Подключение к БД (SQLAlchemy)
├── main.py          # Точка входа FastAPI-приложения
├── models.py        # ORM-модели (Authors, Books с relationship)
├── penalty.py       # Логика штрафов
├── schemas.py       # Pydantic-модели (валидация данных)
└── storage.py       # Слой доступа к данным (CRUD)

alembic/
├── versions/        # Миграции БД
│   ├── 001_create_books_table.py
│   └── 002_create_authors_and_link_books.py
├── env.py           # Конфигурация Alembic
└── script.py.mako   # Шаблон миграций

tests/
├── __init__.py
├── conftest.py      # Фикстуры pytest
├── test_api.py      # Интеграционные тесты API
└── test_penalty.py  # Unit-тесты расчёта штрафов

Dockerfile             # Образ приложения
.dockerignore          # Исключения для сборки
docker-compose.yml     # Оркестрация сервисов
entrypoint.sh          # Скрипт запуска (ожидание БД + миграции)
requirements.txt       # Зависимости проекта
```

---

## Инструкция по сборке и запуску

### Предварительные требования

- [Python](https://www.python.org/downloads/) 3.11+ установлен (для локальной разработки)
- [Docker](https://docs.docker.com/get-docker/) и Docker Compose установлены (для контейнерного запуска)

---

### Запуск через Docker Compose (рекомендуется)

Поднимает два сервиса: FastAPI-приложение и PostgreSQL.

```bash
docker-compose up --build
```

Приложение будет доступно по адресу: http://localhost:8000  
Интерактивная документация (Swagger UI): http://localhost:8000/docs

#### Переменные окружения

| Переменная | Значение по умолчанию | Описание |
|------------|----------------------|----------|
| `DB_USER` | `library_user` | Пользователь PostgreSQL |
| `DB_PASSWORD` | `library_pass` | Пароль PostgreSQL |
| `DB_NAME` | `library_db` | Имя базы данных |

Можно переопределить через `.env` файл или переменные окружения:

```bash
DB_USER=myuser DB_PASSWORD=mypass DB_NAME=mydb docker-compose up --build
```

#### Остановка

```bash
docker-compose down
```

С удалением данных БД:
```bash
docker-compose down -v
```

---

### Задание 1 — REST API для управления книгами и авторами

Приложение предоставляет CRUD-операции для управления книгами и авторами в библиотеке. Реализована нормализованная схема БД: таблицы `authors` и `books` со связью **один-ко-многим** через внешний ключ `author_id`.

#### Схема базы данных

```
┌─────────────────┐         ┌──────────────────┐
│    authors      │         │      books       │
├─────────────────┤         ├──────────────────┤
│ id (PK)         │◄────────┤ id (PK)          │
│ name (UNIQUE)   │   1:N   │ title            │
│ bio             │         │ author_id (FK)   │
│ created_at      │         │ isbn (UNIQUE)    │
└─────────────────┘         │ published_year   │
                            │ is_available     │
                            └──────────────────┘
```

**Индексы:**
- `ix_books_id` — на первичный ключ книг
- `ix_books_title` — ускорение поиска по названию
- `ix_books_author_id` — ускорение выборки по автору
- `ix_authors_id` — на первичный ключ авторов

**Ограничения:**
- `UNIQUE` на `authors.name`
- `UNIQUE` на `books.isbn`
- `ForeignKey` `books.author_id → authors.id` с `ON DELETE RESTRICT`

#### Доступные эндпоинты

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/` | Корневой эндпоинт (информация об API) |
| GET | `/authors` | Получить список всех авторов |
| GET | `/authors/{author_id}` | Получить автора по ID |
| POST | `/authors` | Создать нового автора |
| PUT | `/authors/{author_id}` | Обновить автора |
| DELETE | `/authors/{author_id}` | Удалить автора |
| GET | `/books` | Получить список всех книг |
| GET | `/books/{book_id}` | Получить книгу по ID |
| POST | `/books` | Создать новую книгу |
| PUT | `/books/{book_id}` | Обновить книгу (полностью или частично) |
| DELETE | `/books/{book_id}` | Удалить книгу |

#### Модели данных

**Author:**

| Поле | Тип | Ограничения | Обязательное |
|------|-----|-------------|--------------|
| `id` | integer | Автоинкремент | ✅ (только в ответе) |
| `name` | string | 1–200 символов, уникальное | ✅ |
| `bio` | string | — | ❌ |
| `created_at` | datetime | Автоматически `now()` | ✅ (только в ответе) |

**Book:**

| Поле | Тип | Ограничения | Обязательное |
|------|-----|-------------|--------------|
| `id` | integer | Автоинкремент | ✅ (только в ответе) |
| `title` | string | 1–200 символов | ✅ |
| `author_id` | integer | `≥ 1`, ссылка на `authors.id` | ✅ |
| `isbn` | string | 1–20 символов, только цифры и дефисы, итого 10 или 13 цифр, уникальное | ✅ |
| `published_year` | integer | 1450 – текущий год | ✅ |
| `is_available` | boolean | По умолчанию `true` | ✅ |

#### Примеры запросов и ответов

**Создание автора:**

Запрос:
```bash
curl -X POST "http://localhost:8000/authors" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Лев Толстой",
    "bio": "Русский писатель, автор «Войны и мира»."
  }'
```

Ответ (`201 Created`):
```json
{
  "id": 1,
  "name": "Лев Толстой",
  "bio": "Русский писатель, автор «Войны и мира».",
  "created_at": "2024-01-15T10:30:00"
}
```

**Создание книги (с привязкой к автору):**

Запрос:
```bash
curl -X POST "http://localhost:8000/books" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Война и мир",
    "author_id": 1,
    "isbn": "978-5-699-12014-7",
    "published_year": 1869,
    "is_available": true
  }'
```

Ответ (`201 Created`):
```json
{
  "id": 1,
  "title": "Война и мир",
  "author_id": 1,
  "isbn": "978-5-699-12014-7",
  "published_year": 1869,
  "is_available": true
}
```

Автор не найден (`404 Not Found`):
```json
{
  "detail": "Автор с id=999 не найден"
}
```

**Получение всех книг:**

Запрос:
```bash
curl "http://localhost:8000/books"
```

Ответ (`200 OK`):
```json
[
  {
    "id": 1,
    "title": "Война и мир",
    "author_id": 1,
    "isbn": "978-5-699-12014-7",
    "published_year": 1869,
    "is_available": true
  }
]
```

**Получение книги по ID:**

Запрос:
```bash
curl "http://localhost:8000/books/1"
```

Ответ (`200 OK`):
```json
{
  "id": 1,
  "title": "Война и мир",
  "author_id": 1,
  "isbn": "978-5-699-12014-7",
  "published_year": 1869,
  "is_available": true
}
```

Книга не найдена (`404 Not Found`):
```json
{
  "detail": "Книга с id=999 не найдена"
}
```

**Частичное обновление книги:**

Запрос:
```bash
curl -X PUT "http://localhost:8000/books/1" \
  -H "Content-Type: application/json" \
  -d '{"title": "Война и мир (новое издание)"}'
```

Ответ (`200 OK`):
```json
{
  "id": 1,
  "title": "Война и мир (новое издание)",
  "author_id": 1,
  "isbn": "978-5-699-12014-7",
  "published_year": 1869,
  "is_available": true
}
```

**Удаление книги:**

Запрос:
```bash
curl -X DELETE "http://localhost:8000/books/1"
```

Ответ: `204 No Content` (тело пустое).

**Удаление автора:**

Запрос:
```bash
curl -X DELETE "http://localhost:8000/authors/1"
```

Ответ: `204 No Content`.

Автор не найден (`404 Not Found`):
```json
{
  "detail": "Автор с id=999 не найден"
}
```

#### Валидация данных

Pydantic-модели автоматически проверяют входные данные:

**Author:**
- `name` — строка от 1 до 200 символов, уникальная
- `bio` — строка (опционально)

**Book:**
- `title` — строка от 1 до 200 символов
- `author_id` — целое число ≥ 1 (ссылка на существующего автора)
- `isbn` — строка от 1 до 20 символов, содержит только цифры и дефисы, итого 10 или 13 цифр, уникальная
- `published_year` — целое число от 1450 до текущего года
- `is_available` — булево значение (по умолчанию `true`)

Пример ошибки валидации:
```bash
curl -X POST "http://localhost:8000/books" \
  -H "Content-Type: application/json" \
  -d '{"title": "", "author_id": 1, "isbn": "978-0-123456-78-9", "published_year": 2020}'
```
Ответ: `422 Unprocessable Entity` с описанием ошибок.


---

## Тесты

Проект покрыт интеграционными и unit-тестами.

| Файл | Количество тестов | Описание |
|------|-------------------|----------|
| `tests/test_api.py` | 37 | CRUD-операции API (авторы + книги), валидация, граничные случаи |
| `tests/test_penalty.py` | 22 | Расчёт штрафов, граничные случаи, валидация типов и дней |

#### Запуск тестов

Тесты запускаются внутри Docker-контейнера приложения.

Все тесты:
```bash
docker-compose exec app pytest tests/ -v
```

Только API:
```bash
docker-compose exec app pytest tests/test_api.py -v
```

Только штрафы:
```bash
docker-compose exec app pytest tests/test_penalty.py -v
```

С отчётом о покрытии:
```bash
docker-compose exec app pytest tests/ -v --cov=app --cov-report=term-missing
```

#### Проверяемые сценарии (API)

**Авторы:**
- Создание автора с валидными данными
- Валидация пустого имени
- Получение списка авторов (пустой / с данными)
- Получение автора по ID (успех / не найден)
- Обновление автора (полное / частичное)
- Обновление несуществующего автора
- Удаление автора (успех / не найден)

**Книги:**
- Корневой эндпоинт возвращает метаданные API
- Получение пустого списка книг
- Создание книги с валидными данными
- Привязка книги к существующему автору
- Ошибка при привязке к несуществующему автору
- Валидация пустых строк (`title`, `isbn`)
- Валидация года издания (будущий год, слишком старый)
- Валидация ISBN (буквы, неверная длина)
- Получение книги по ID (успех / не найдена)
- Частичное обновление книги
- Обновление несуществующей книги
- Обновление `author_id` на несуществующего автора
- Удаление книги (успех / не найдена)
- Инкремент ID при создании нескольких книг
- Проверка структуры ответа

---

### Рефакторинг модуля расчёта штрафов

Исходная реализация представляла собой монолитную функцию `calc(data)`, принимающую кортеж с неинформативными индексами и содержащую дублирующуюся логику расчёта для каждого типа книги.

| Проблема | Исправление | Зачем |
|----------|-------------|-------|
| Магические числа | Вынесены константы (`WEEK_THRESHOLD_DAYS`, `MONTH_THRESHOLD_DAYS`) и тарифы в словари (`DAILY_RATE`, `WEEKLY_SURCHARGE`, `MONTHLY_SURCHARGE`) | Изменение тарифа в одном месте, а не в дублях кода |
| Неинформативные аргументы | Создан `PenaltyInput` (`@dataclass`) с именованными полями (`overdue_days`, `book_type`) | Читаемость и валидация на уровне типов |
| «Сырые» строки типов | `BookType(str, Enum)` | Типобезопасность, автодополнение IDE, единый источник истины |
| Дублирование логики | Единая функция `calculate_penalty` без повторения кода для каждого типа | Переиспользование, проще тестировать |
| `print` в бизнес-логике | Убраны все `print`, логирование вынесено за пределы модуля | Чистая бизнес-логика без сайд-эффектов |
| Ручная работа с файлом | Убрана запись в файл из модуля расчёта | Разделение ответственности: расчёт ≠ логирование |
| Отсутствие валидации | `ValueError` при `overdue_days < 0` и при неизвестном `book_type` | Раннее обнаружение ошибок, предсказуемое поведение |
| Нет type hints | Добавлены аннотации ко всем функциям и константам | Статический анализ, самодокументируемость |
| Неясное имя `calc` | Переименовано в `calculate_penalty` + обёртка `calculate_penalty_from_raw` | Читаемость и обратная совместимость |

---
